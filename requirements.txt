# The CLI itself has no hard runtime dependencies - frameworks are installed
# into the generated Docker image or portable package instead.
#
# This file covers a full local development environment (PyTorch demo model,
# sample image generation and the test suite). For a leaner install, use the
# extras defined in pyproject.toml, e.g. `pip install -e ".[onnx]"`.
torch>=2.0.0
torchvision>=0.15.0
pillow>=10.0.0
numpy>=1.24.0
pytest>=7.4.0

# Optional - install only if you package models in these formats:
# tensorflow>=2.13.0
# onnxruntime>=1.16.0
# scikit-learn>=1.3.0
# joblib>=1.3.0
